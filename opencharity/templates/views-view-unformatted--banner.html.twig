<section id="banner">
    <div class="container">
        {% if title %}
            <h2>{{ title }}</h2>
        {% endif %}
        {% for row in rows %}
             {{ row.content }}
        {% endfor %}
    </div>
</section>

